import actions from './actions';

export const updateCurrentMessage = actions.updateCurrentMessage;

export const toggleMessageBoard = actions.toggleMessageBoard;

export default {
	updateCurrentMessage,
	toggleMessageBoard
};
