import actions from './actions';

export const openModal = actions.openModal;
export const closeModal = actions.closeModal;
export const setSelectedEvent = actions.setSelectedEvent;
export const clearSelectedEvent = actions.clearSelectedEvent;

export default {
	openModal,
	closeModal,
	setSelectedEvent,
	clearSelectedEvent
};
